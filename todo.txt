# temporary file (future readme)

add mysql support
    get discord key from table or from config file
add skip command

required python packages (pip):
    - discord.py[voice]
    - yt-dlp

required libs on macOS:
    - ffmpeg (added to $PATH)
    - opius (copied from git repository and maked)